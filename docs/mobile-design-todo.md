# Mobile Design Todo

Status for the Create Story mobile polish pass.

- [x] Add enough bottom safe-area padding so scrollable content is not trapped behind the floating tab bar.
- [x] Reduce and reposition the bottom tab bar so it consumes less vertical space.
- [x] Keep the primary generate action clear of the persistent tab bar.
- [x] Shorten the primary CTA label so it fits reliably on small mobile widths.
- [x] Increase the story prompt field height and tune its padding and type scale.
- [x] Remove unfinished `..` character placeholders.
- [x] Make the add-character tile show an intentional plus affordance instead of an `A` initial.
- [x] Strengthen the selected-character state with a heavier border and check badge.
- [x] Remove decorative sprigs from the form screen so they do not cross inputs or actions.
- [x] Tighten top spacing so the greeting does not push the rest of the form into the tab bar.
- [x] Reduce the visual weight of story type controls and align their radius with the rest of the form.
- [x] Make the below-fold content start cleanly away from the floating navigation.
